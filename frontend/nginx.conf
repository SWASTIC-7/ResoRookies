server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    # You could add more configurations here if needed
}

